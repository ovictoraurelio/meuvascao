// Esquema Drizzle do Meu Vascão (dialeto SQLite/D1).
// As tabelas entram na fatia F3 seguindo o modelo de dados aprovado no plano.
// Regras de portabilidade para um eventual Postgres: timestamps em integer (ms), booleanos em
// integer mode boolean, IDs em text, nenhum SQL de dialeto fora de um ponto central.
export {};
